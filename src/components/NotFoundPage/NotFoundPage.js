import React from "react";


const NotFoundPage = () => (
  <div className="container">
    <header className="jumbotron">
      <div>Not found</div>
    </header>
  </div>
);

export default NotFoundPage;